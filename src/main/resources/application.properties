spring.application.name=HealthManagement

# Database Config (Injected via Environment Variables)
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Flyway — reuses spring.datasource connection automatically, no need to repeat credentials
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.jpa.defer-datasource-initialization=true
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1
logging.level.org.flywaydb=DEBUG

# Security Config (JWT)
jwt.secret=${JWT_SECRET}
jwt.expiration=18000000


# Google AI (Uncomment when ready to use Gemini)
# spring.ai.google.genai.api-key=${GEMINI_API_KEY}
# spring.ai.google.genai.chat.options.model=gemini-2.5-flash
# spring.ai.google.genai.chat.options.temperature=0.7